<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BirthdayTrackerV2" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/button_text_color</item>
        <item name="android:colorBackground">@color/surface</item>
        <item name="android:textColor">@color/button_text_color</item>

        <!-- Status bar color. -->
        <!--item name="android:statusBarColor">?attr/colorPrimaryVariant</item-->
        <item name="android:statusBarColor">@color/button_text_color</item>
        <!-- Customize your theme here. -->
    </style>
</resources>